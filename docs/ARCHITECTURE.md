# 🏗️ Архитектура приложения

## 📋 Обзор

**facemassage.net** - Next.js приложение для онлайн-бронирования массажных услуг с интеграцией Google Calendar и Google Sheets.

### Технологический стек

- **Frontend**: Next.js 15, React 19, TypeScript, TailwindCSS
- **Backend**: Next.js API Routes, Node.js runtime
- **Базы данных**: 
  - Google Calendar (бронирования)
  - Google Sheets (процедуры, согласия GDPR)
  - Upstash Redis (кэширование, rate limiting)
- **Внешние сервисы**:
  - Google Calendar API
  - Google Sheets API
  - Cloudflare Turnstile (защита от ботов)
  - Sentry (мониторинг ошибок)

---

## 📁 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API endpoints
│   │   ├── book/          # Создание бронирования
│   │   ├── bookings/      # Управление бронированиями
│   │   ├── consents/      # GDPR операции
│   │   ├── procedures/    # Список услуг
│   │   └── availability/  # Проверка доступности
│   ├── privacy/           # Политика конфиденциальности
│   └── page.tsx           # Главная страница
│
├── components/            # React компоненты
│   ├── booking-management/  # Управление бронированиями
│   ├── hooks/             # Custom React hooks
│   ├── providers/         # Context providers
│   └── ui/                # UI компоненты
│
├── lib/                   # Бизнес-логика
│   ├── api/               # API helpers
│   │   ├── error-handler.ts
│   │   └── error-responses.ts
│   ├── cache/             # Кэширование
│   │   └── sheets-cache.ts
│   ├── google/            # Google API интеграции
│   │   ├── calendar.ts
│   │   ├── sheets.ts
│   │   └── consents/      # GDPR операции
│   ├── utils/             # Утилиты
│   │   ├── phone-normalization.ts
│   │   ├── string-normalization.ts
│   │   └── date-formatters.ts
│   ├── validation/        # Валидация
│   │   ├── client-validators.ts  # Клиент (0 зависимостей)
│   │   └── api-schemas.ts        # Сервер (Zod)
│   ├── availability.ts    # Логика доступности
│   ├── booking-helpers.ts # Хелперы бронирований
│   └── logger.ts          # Логирование
│
├── types/                 # TypeScript типы
│   ├── api-responses.ts   # API ответы
│   └── google-calendar.ts # Google Calendar типы
│
└── styles/
    └── globals.css        # Глобальные стили
```

---

## 🔄 Основные потоки данных

### 1. Поток создания бронирования

```
Пользователь → Выбор процедуры → Выбор даты → Выбор времени → Форма → API → Google Calendar
                     ↓                ↓              ↓            ↓       ↓
              GET /procedures   GET /availability  GET /day/:date  POST /book  Calendar Event
                     ↓
              Google Sheets ← Кэш (1 час)
```

**Шаги:**

1. **Выбор процедуры** (`ProcedureSelect.tsx`)
   - Загрузка из `/api/procedures`
   - Кэш: 1 час (React Query)
   - Источник: Google Sheets

2. **Выбор даты** (`DayCalendar.tsx`)
   - Загрузка доступности из `/api/availability`
   - Проверка свободных окон для процедуры
   - Кэш: 10 минут

3. **Выбор времени** (`SlotsList.tsx`)
   - Загрузка слотов из `/api/day/:date`
   - FreeBusy проверка в Google Calendar
   - Кэш: 5 минут

4. **Заполнение формы** (`BookingForm.tsx`)
   - Клиентская валидация (instant feedback)
   - Turnstile токен
   - Проверка согласий GDPR

5. **Создание бронирования** (`POST /api/book`)
   - Серверная валидация (Zod)
   - Rate limiting
   - Проверка доступности
   - Создание события в Calendar
   - Сохранение согласий в Sheets

### 2. Поток управления бронированием

```
Поиск → Верификация → Выбор действия → API → Google Calendar
  ↓           ↓              ↓           ↓
Search    Access Check   Edit/Cancel  Update/Delete
```

**Операции:**

- **Поиск** (`POST /api/bookings/search`): Телефон + Имя
- **Изменение времени** (`POST /api/bookings/update-time`): Правило 24 часа
- **Изменение процедуры** (`POST /api/bookings/update-procedure`)
- **Отмена** (`POST /api/bookings/cancel`)

**Правила доступа:**
- Телефон + Имя должны совпадать
- Изменение: >24 часа до визита
- Отмена: в любое время

### 3. Поток GDPR операций

```
Пользователь → Turnstile → API → Google Sheets
                    ↓         ↓
              Bot Protection  GDPR Operations
```

**Операции:**

- **Проверка согласий** (`POST /api/consents/check`)
- **Отзыв согласий** (`POST /api/consents/withdraw`)
- **Удаление данных** (`POST /api/consents/erase`)
- **Экспорт данных** (`POST /api/consents/export`)

**GDPR гарантии:**
- IP маскирование (SHA-256)
- Анонимизация вместо удаления (сохранение истории)
- Audit trail для всех операций

---

## 🗄️ Интеграции

### Google Calendar API

**Функции:**
- Создание событий (бронирования)
- Обновление событий
- Удаление событий
- FreeBusy запросы
- Поиск по метаданным

**Метаданные бронирования** (extendedProperties):
```typescript
{
  phone: '+48123456789',
  customerName: 'Jan Kowalski',
  customerEmail: 'jan@example.com',
  procedureId: 'proc_60min',
  procedureName: 'Masaż twarzy',
  procedureDurationMin: '60',
  procedurePricePLN: '200'
}
```

### Google Sheets API

**Sheets:**
1. **Procedures** (Процедуры)
   - Колонки: id, name_pl, duration_min, price_pln, active
   - Кэш: 10 минут (in-memory)

2. **Consents** (Согласия GDPR)
   - Колонки: phone, email, name, consentDate, privacy, terms, notifications
   - GDPR колонки: withdrawnDate, erasureDate
   - Кэш: 2 минуты

3. **Schedule** (Расписание)
   - Рабочие часы
   - Выходные дни
   - Исключения

### Upstash Redis

**Использование:**
- **Rate limiting**: 10 req/min, 50 req/hour
- **Idempotency**: Предотвращение дублирования бронирований
- **Cache**: Результаты FreeBusy запросов

**TTL:**
- Rate limit counters: 1 час
- Idempotency keys: 10 минут
- FreeBusy cache: 5 минут

### Cloudflare Turnstile

**Защита endpoints:**
- `POST /api/book` - всегда
- `POST /api/consents/*` - для GDPR операций
- `POST /api/bookings/cancel` - опционально

---

## 🔐 Безопасность

### Валидация

**Клиент** (`client-validators.ts`):
- Легковесные функции без зависимостей
- Instant feedback для UX
- Телефон, email, имя

**Сервер** (`api-schemas.ts`):
- Zod схемы для всех API
- Строгая типизация
- Защита от инъекций

### Обработка ошибок

**Централизованная** (`error-handler.ts`):
- Типизированные ошибки (`ApiError`)
- Автоматическое логирование
- Sentry интеграция (production)

**Коды ошибок**:
- 400: Валидация
- 403: Turnstile / Access denied
- 404: Booking not found
- 409: Slot conflict
- 422: Business logic (24h rule)
- 429: Rate limited
- 500: Server error

---

## 🚀 Производительность

### Кэширование

**React Query** (клиент):
- Procedures: 1 час
- Availability: 10 минут
- Day slots: 5 минут
- GC time: 30 минут

**In-Memory Cache** (сервер):
- Procedures: 10 минут
- Schedule: 5 минут
- Consents: 2 минуты

**Redis** (распределенный):
- FreeBusy: 5 минут
- Rate limits: 1 час

### Оптимизации

- **Code splitting**: Динамические импорты
- **Мемоизация**: useMemo для тяжелых вычислений
- **Debouncing**: Phone validation (500ms)
- **Prefetching**: Procedures на mount

---

## 📊 Мониторинг

### Логирование

**Pino logger**:
- Структурированные JSON логи
- Уровни: debug, info, warn, error
- Контекст: module, ip, userId

### Error tracking

**Sentry**:
- Автоматический capture ошибок
- Source maps для production
- Breadcrumbs для контекста

---

## 🧪 Тестирование

**Структура тестов:**
```
tests/
├── lib/
│   ├── utils/              # Unit тесты утилит
│   ├── google/             # Unit тесты Google API
│   ├── booking-helpers.test.ts
│   └── availability.test.ts
└── app/api/                # Integration тесты API
```

**Фреймворк**: Vitest
**Покрытие**: Критическая бизнес-логика

---

## 📝 Следующие шаги

**TODO:**
- [ ] Интеграция sheets-cache.ts в readProcedures()
- [ ] Миграция всех API на handleApiError()
- [ ] Добавление integration тестов для API
- [ ] Email уведомления (опционально)
